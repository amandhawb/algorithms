# 1. Which data format is accepted by Azure AI Search when you're pushing data to the index? 
# CSV.
# SQL.
# JSON. --> Correct

# 2. Which explanation best describes an indexer and an index? 
# An indexer converts documents into JSON and forwards them to a search engine for indexing. --> Correct
# An indexer can be used instead of an index if the files are already in the proper format.
# An indexer is only used for AI enrichment and skillset execution.

# 3. If you set up a search index of written news documents without including any skillsets, what information would you still be able to query? 
# The sentiment.
# The full-text. --> Correct
# The AI-generated image captions.